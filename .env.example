# Скопируй в .env и заполни значения (файл .env в git не попадает)

BOT_TOKEN=
NOTIFICATION_CHAT_ID=
CHANNEL_LINK=

# Путь к PDF гайду (если не задан — ищется guide.pdf в папке бота)
# PDF_FILE_PATH=guide.pdf
